# 快速开始指南

欢迎！本指南将帮助您快速开始使用和定制您的艾维果文档网站。

## 🚀 立即开始

### 1. 本地开发

```bash
# 安装依赖（如果还没有安装）
npm install

# 启动开发服务器
npm start
```

打开浏览器访问 `http://localhost:3000`，您将看到网站首页。

### 2. 查看和编辑内容

#### 编辑文档

文档位于 `docs/` 目录：
- `docs/intro.md` - 欢迎页面
- `docs/getting-started.md` - 快速开始指南

您可以：
- 修改现有文档
- 添加新的 Markdown 文件
- 侧边栏会自动根据文件结构生成

#### 编辑博客

博客文章位于 `blog/` 目录：
- `blog/2024-12-23-welcome.md` - 欢迎文章
- `blog/2024-12-23-first-steps.md` - 快速开始文章

创建新博客文章：
1. 在 `blog/` 目录创建新文件，命名格式：`YYYY-MM-DD-文章标题.md`
2. 添加 front matter（文件头部的 YAML 配置）
3. 编写内容

示例：

```markdown
---
slug: my-new-post
title: 我的新文章
authors:
  - ivygo_team
tags: [教程, 功能]
---

这是文章内容...
```

### 3. 自定义网站

#### 修改网站信息

编辑 `docusaurus.config.ts`：
- `title` - 网站标题
- `tagline` - 网站标语
- `url` 和 `baseUrl` - 网站 URL 配置

#### 修改样式

编辑 `src/css/custom.css` 来自定义颜色、字体等样式。

#### 修改首页

编辑 `src/pages/index.tsx` 来自定义首页内容。

#### 修改 Logo

替换 `static/img/logo.svg` 为您的 Logo。

### 4. 构建和测试

```bash
# 构建生产版本
npm run build

# 预览构建结果
npm run serve
```

### 5. 部署到 GitHub Pages

#### 首次部署

1. 确保代码已推送到 GitHub
2. 在 GitHub 仓库设置中启用 GitHub Pages（使用 GitHub Actions）
3. 推送代码到 `main` 分支，GitHub Actions 会自动部署

详细步骤请查看 [DEPLOYMENT.md](./DEPLOYMENT.md)

## 📁 项目结构

```
demoblog/
├── blog/                    # 博客文章
│   ├── authors.yml          # 作者配置
│   └── *.md                 # 博客文章
├── docs/                    # 文档
│   ├── intro.md            # 欢迎页面
│   └── getting-started.md  # 快速开始
├── src/
│   ├── components/         # React 组件
│   │   └── HomepageFeatures/
│   ├── css/                # 样式文件
│   │   └── custom.css
│   └── pages/              # 自定义页面
│       └── index.tsx       # 首页
├── static/                 # 静态资源
│   └── img/               # 图片
├── .github/
│   └── workflows/         # GitHub Actions
│       └── deploy.yml
├── docusaurus.config.ts   # 主配置文件
├── sidebars.ts            # 侧边栏配置
└── package.json
```

## 🎨 常用操作

### 添加新文档

1. 在 `docs/` 目录创建 `.md` 文件
2. 文件会自动出现在侧边栏中

### 添加新博客

1. 在 `blog/` 目录创建 `YYYY-MM-DD-标题.md` 文件
2. 添加 front matter 配置
3. 编写内容

### 修改导航栏

编辑 `docusaurus.config.ts` 中的 `navbar.items` 配置。

### 修改页脚

编辑 `docusaurus.config.ts` 中的 `footer` 配置。

## 📚 学习资源

- [Docusaurus 官方文档](https://docusaurus.io/docs)
- [Markdown 语法指南](https://docusaurus.io/docs/markdown-features)
- [React 组件开发](https://docusaurus.io/docs/swizzling)

## ❓ 常见问题

### 本地开发时页面无法加载

- 确保端口 3000 未被占用
- 尝试清除缓存：`npm run clear && npm start`

### 构建失败

- 检查是否有语法错误
- 确保所有依赖已安装：`npm install`
- 查看错误日志定位问题

### 部署后样式丢失

- 检查 `baseUrl` 配置是否正确
- 确保 GitHub Pages 设置正确

## 🎯 下一步

1. ✅ 熟悉项目结构
2. ✅ 修改示例内容
3. ✅ 添加您的产品文档
4. ✅ 发布博客文章
5. ✅ 自定义样式和主题
6. ✅ 部署到 GitHub Pages

---

祝您使用愉快！如有问题，请查看文档或提交 Issue。

